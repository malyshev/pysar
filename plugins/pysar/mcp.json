{
  "mcpServers": {
    "pysar": {
      "type": "stdio",
      "command": "${userHome}/.local/bin/pysar",
      "args": [
        "serve"
      ],
      "env": {
        "PYSAR_PROJECT_ROOT": "${workspaceFolder}"
      }
    }
  }
}
