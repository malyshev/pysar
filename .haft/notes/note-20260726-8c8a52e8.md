---
id: note-20260726-8c8a52e8
kind: Note
version: 1
status: active
mode: note
valid_until: 2026-10-24T10:39:24Z
created_at: 2026-07-26T10:39:24Z
updated_at: 2026-07-26T10:39:24Z
---

# 

## Rationale

Concrete, checkable findings, not vibes:

1. Readability-fold-in pays off measurably. Pysar's staff-edit absorbed the readability check POC defers to an unbuilt SHARPEN stage -- and it shows: POC's current opener paragraph (line 9 of its staff-edited article.md) crams "what you were trying to fix" + inherited constraints + silent-failure + slow-build into one dense paragraph with two citations, exactly the "doing two jobs at once" pattern Pysar's own staff-edit explicitly splits apart (verified Pysar split the cache-ordering, multi-stage, and "what to skip" paragraphs into 2-3 focused paragraphs each, per its own changelog and confirmed by reading the text). At this equivalent pipeline depth, Pysar's artifact is closer to finished specifically because the architecture decision (fold readability into staff-edit rather than defer it) is already paying off in the actual text, not just in theory.

2. Pysar's multi-stage section shows a full runnable code example (build stage + RUN npm run build + COPY --from=build .../dist) while POC's stays prose-only ("docs show the pattern..."). More concrete and actionable, but it required Pysar's own staff-edit to catch and fix a real logical gap (the code jumps to a dist/ output the naive single-stage example never set up) -- confirmed the fix landed: "build tooling your running app never touches once dependencies are installed and -- if your app has one -- the build step has run" / "runs whatever build step your app has (a bundler, a TypeScript compile)". POC sidesteps the equivalent risk by simply not showing a concrete multi-stage code snippet -- fewer bugs to catch, but also less directly copyable material for the reader.

3. Pysar retains the docker-compose v1/v2 EOL content (support ended after June 2023) that POC's piece doesn't cover at all, at either draft or staff-edit stage -- a real, currently-relevant differentiator.

4. Fair point the other way: POC's staff-edit pass added three genuinely new pieces of substantive failure-mode content this round (EXPOSE-vs--p footgun, anonymous /app/node_modules volume-collision explanation, sharper non-root USER boundary wording) -- more new substance added at THIS stage than Pysar's staff-edit added. Not a staff-edit quality gap on Pysar's side though: Pysar's own changelog recorded "no changes needed" for failure-mode coverage because its draft stage had already covered that ground (privileged-port edge case, Alpine/musl caveat, digest-pinning) -- the two systems distributed the same kind of editorial work across different pipeline stages, not one system skipping work the other did.

5. Word count: Pysar 2064 vs POC 1604 (~29% longer) -- earned length (more granular subsections, the extra compose-EOL section, denser per-point explanation), not padding, though worth flagging it's pushing past what POC's own "quick-insight" reading-session band would allow (Pysar has no such formal field to violate).

6. Voice difference reconfirmed, not re-litigated: Pysar's staff-edit.md has zero hits against its own voice.md banned_phrases (re-scanned, same as prior finding); POC keeps its punchier prior writing POC voice (named "Cargo-Cult Layer Tax" framework, "Pro Tip" callout, couplet close). Both executing their own configured/fixed voice correctly.

Declined (consistently, third time) to give a percentage-quality verdict. On the concrete, checkable dimensions above, Pysar's output holds up well at this checkpoint -- arguably ahead on structural finish and concrete actionability specifically because its readability pass already ran -- while POC's punchier voice and this round's new failure-mode substance are real, genuine strengths on its side.
