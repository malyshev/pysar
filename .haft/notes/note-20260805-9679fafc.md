---
id: note-20260805-9679fafc
kind: Note
version: 1
status: active
title: Deferred: eliminate per-call web permission prompts for ps-research/ps-factcheck (parked, not decided)
mode: note
valid_until: 2026-11-03T05:47:55Z
created_at: 2026-08-05T05:47:55Z
updated_at: 2026-08-05T05:47:55Z
---

# Deferred: eliminate per-call web permission prompts for ps-research/ps-factcheck (parked, not decided)

## Observations

- Operator wants prob-20260805-c8ab2784 and its portfolio sol-20260805-332b8e21 parked as backlog -- real enough to keep, not urgent enough to decide now.
- settings.json's permissions.allow is Claude-Code-specific config -- not portable to Cursor/Codex/other hosts Pysar has already committed to as host-agnostic (dec-20260719-fa0366dd's own MCP-wildcard precedent). This weakens V1 (pre-approve native WebFetch/WebSearch) and V4 (split policy): both would need per-host reimplementation in each host's own permission dialect, with no guarantee an equivalent lever exists in every host.
- V3's MCP-mediated search would require the operator to provision and pay for a real third-party search API (Google Custom Search, Bing, Brave, SerpAPI) since Pysar's Go server has no web index of its own -- unlike WebFetch (plain HTTP GET, free, no index needed) or native WebSearch (already free via the host today). Real ongoing cost/setup trade, not a rounding error.
- Current lean, not yet decided: V2 (MCP-mediated fetch only, native WebSearch left as-is for now) as a stepping stone -- reuses this project's own established pattern for this exact class of problem (mediate friction through the wildcard-approved MCP server instead of pre-approving a raw tool), costs nothing, doesn't force the paid-search-API decision before the operator is ready.
- Nothing decided or implemented yet -- no code changes, no settings.json edit. Next step when revisited: run /h-compare with host-portability added as an explicit dimension, or the operator may already know which way to go.


## Anchors

- about `prob-20260805-c8ab2784`
- about `sol-20260805-332b8e21`
- relates_to `dec-20260719-fa0366dd`

## Affected Files

- `cmd/pysar/assets/claude/settings.json`
