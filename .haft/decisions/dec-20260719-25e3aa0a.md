---
id: dec-20260719-25e3aa0a
kind: DecisionRecord
version: 1
status: active
title: Real Claude init scaffold -- corrected default file-set prediction
mode: tactical
created_at: 2026-07-19T15:03:17Z
updated_at: 2026-07-19T15:03:17Z
links:
  - ref: prob-20260719-366c0c9d
    type: based_on
---

# Real Claude init scaffold -- corrected default file-set prediction

## 1. Problem Frame

**Signal:** Decision dec-20260718-fe926229 needs re-evaluation: Prediction 1's threshold text ("only .pysar/project exists on disk") is stale -- real default init now legitimately writes 4 files (.pysar/project, CLAUDE.md, .claude/settings.json, .mcp.json) per two later, separately-decided changes (dec-20260718-4b222794, dec-20260719-fa0366dd). The selected variant itself (real scaffold, not empty mkdir) is still correct; only the prediction wording needs correcting via a superseding decision.

## 2. Decision

**Selected:** Real Claude init scaffold -- corrected default file-set prediction

**Selection policy:** N/A -- wording correction to an already-accepted variant, not a new comparison.

**Why selected:** dec-20260718-fe926229's selected variant (a real scaffold, not empty mkdir) is unchanged and still correct -- this corrects only its stale prediction-1 threshold text ("only .pysar/project exists"), which never accounted for two later, separately-decided, legitimate additions to the default scaffold: dec-20260718-4b222794's host-scaffold follow-through (CLAUDE.md) and dec-20260719-fa0366dd's MCP serve wiring (.claude/settings.json, .mcp.json). A real binary smoke test on 2026-07-19 confirms these 4 files, and only these 4, are written by default init. No new comparison or architecture choice is being made here.


**Invariants:**
- pysar init never writes author-facing prose template files (inherited from dec-20260718-4b222794)
- .pysar/project is valid, parseable data containing at minimum schema_version and host fields
- pysar init never overwrites an existing file -- it fails loudly or skips rather than clobbering

## 3. Rationale

**Counterargument:** The original 2026-07-18 Impact Measurement already marked prediction 1 "accepted" despite the real behavior (2 files) not literally matching its own threshold text (1 file) -- amending the wording now, after a 3rd and 4th file were added, could look like moving the goalposts after the fact instead of admitting the measurement was already loose. Mitigation: this correction names exactly which two later, independently-decided changes expanded the set and when, so the history stays traceable rather than quietly rewritten.

**Selected variant weakest link:** Same as the original decision: the exact contents of the non-.pysar host files (CLAUDE.md, settings.json, mcp.json) remain provisional per their own originating decisions, not re-litigated here.

**Rejected alternatives:**
| Variant | Verdict | Reason |
|---------|---------|--------|
| Real Claude init scaffold -- corrected default file-set prediction | **Selected** | dec-20260718-fe926229's selected variant (a real scaffold... |
| Leave the original prediction wording unchanged and let it stand as permanently failed/partial | Rejected | Would misrepresent working, intentional, already-decided-elsewhere behavior as broken. The invariant this decision actually protects -- no author-facing prose stub files -- still holds; only the file-count in the threshold text was ever wrong. |

**Predictions:**
| Claim | Observable | Threshold |
|-------|------------|-----------|
| pysar init [dir] (no flag or --claude) creates the accepted default scaffold set -- .pysar/project (schema_version + host fields), CLAUDE.md, .claude/settings.json, .mcp.json -- and writes nothing outside that set | go install, then run pysar init against a scratch directory and inspect its full contents | Exactly these four files exist; .pysar/project parses and contains schema_version and host fields; no author-prose stub files (idea.md/draft.md) are present |
| pysar init --cursor / --codex still fail loudly with a 'not yet supported' message and write nothing, unchanged from dec-20260718-8278c494 | Run pysar init --cursor and pysar init --codex against scratch directories | Both exit non-zero with a clear message; no files or directories are created |
| go test ./cmd/pysar passes | go test ./cmd/pysar -v | All tests pass, zero failures |

## 4. Consequences

**Rollback plan:**
Triggers:
- The default scaffold file set changes again without a corresponding decision update
Steps:
1. Re-run the real-binary smoke test against a scratch dir and update this decision's prediction-1 threshold to match
Blast radius: cmd/pysar/main.go (scaffoldClaude), cmd/pysar/init_test.go, documentation of default init behavior only -- no code change implied by this correction itself

**Affected files:** cmd/pysar/main.go, cmd/pysar/init_test.go, .pysar/**
