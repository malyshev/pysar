---
id: prob-20260725-747fcd29
kind: ProblemCard
version: 1
status: active
title: /ps-intake: scaffold brief/outline/angles from an operator idea or draft (v2 -- profile-driven defaults)
mode: standard
created_at: 2026-07-25T06:13:41Z
updated_at: 2026-07-25T06:13:41Z
---

# /ps-intake: scaffold brief/outline/angles from an operator idea or draft (v2 -- profile-driven defaults)

## Signal

Reframe of prob-20260725-53b2d8ea (deprecated) after discussion resolved how intake should handle sparse or broad input. The prior frame treated 'how does the agent force correct understanding of the idea' as an open multi-variant design question, including an explicit confirmation-gate as one candidate. That was wrong on two counts, established through concrete test cases with the operator: (1) audience/register/depth are not intake's judgment call at all -- Pysar already has this answer on file the moment an author has run /ps-voice or /ps-style: the same input 'write something about AI' means a plain-language explainer for a general audience if the profile says so, a peer-technical piece if the profile says the author is a cybersecurity practitioner, and a child-appropriate piece if the profile says the author writes for kids. Guessing generic-broad-default when a profile already answers the question is itself a misread. (2) A blocking clarification question is not the right response to merely broad or vague input ('I want water' does not require 'still, sparkling, cold, or with lemon' -- bring water; correction after the fact is cheap, interrogating up front is not). It IS the right response to genuinely degenerate input the operator explicitly called 'absolutely stupid' -- input with no extractable semantic content at all (gibberish, a stray word, explicitly self-contradictory instructions) where even a profile-informed guess has nothing to anchor to.

## Problem Type

synthesis

## Constraints

- Audience/register/depth defaults are read from the author's existing voice/style profile (.pysar/voice.md, .pysar/style.md) when one exists -- intake never asks 'who is this for' and never silently substitutes a generic-public default when a profile already answers it. This is an invariant, not a comparison axis.
- A blocking clarification question fires ONLY for input with no extractable semantic content even combined with the profile (empty/gibberish/single stray word) or input containing explicitly self-contradictory instructions (the operator's own words demand two incompatible things). Broad, vague, or topic-only input is NOT grounds for a question -- it gets the most likely default, stated declaratively, non-blocking. This replaces the prior frame's 'confirm if ambiguous' axis entirely.
- When no voice/style profile exists yet (author hasn't onboarded), intake still must not block-and-interrogate -- falls back to the broadest generalist default (general-audience explainer register), stated declaratively, same as any other resolvable default.
- Topic-scope-within-domain (e.g. 'more secure' naming a domain but not a specific angle within it) is resolved by picking the single most likely angle -- informed by the profile's stated expertise/topic-family if present, and by prior pieces via the memory engine (dec-20260718-404573a2) if queryable at intake time to avoid re-pitching an already-covered angle -- stated as an assumption, never asked.
- Must plug into dec-20260718-20a08f83's precondition-guarded pass mechanism as its own pass (produced-artifact state as ArtifactKind refs, own Precondition check) and append to dec-20260718-6ba897d4's run-log on a successful run -- real wiring, not a stub.
- No author registry, --author flag, or per-author resolution -- prob-20260718-97b5a2a3 already excludes the multi-author model from scope.
- No fixed/operator-supplied word-count cap on the brief -- length follows from the idea's actual complexity, not a --length=N ceiling.
- Storage location/topology for the produced artifacts is governed by prob-20260718-f6ac8127, still ACTIVE and UNDECIDED -- this remains a genuinely open dependency for exploration to take an explicit position on, not something to silently assume.
- a prior writing POC ([prior-writing-POC]/.cursor/commands/w-intake.md + .cursor/skills/article-intake/SKILL.md) is CL1-capped inspiration per dec-20260718-eafefad2's reference-agnosticism gate -- its content-contract shape (brief/outline/angles/sources-stub) and its single restate-the-stake step are legitimate to port; its author-registry coupling and fixed --length flag are the two named, deliberate divergences.
- No web search / synthesized research at intake time, mirroring the POC's own INTAKE-phase rule -- matches Pysar's already-stated author-practitioner-voice priority.

## Acceptance

A ProblemCard + SolutionPortfolio exist such that the operator can /h-compare and /h-decide among variants that differ ONLY on the axes that are genuinely still open: (A) storage-topology stance given prob-20260718-f6ac8127 is undecided (write to a provisional, clearly-marked location now vs. defer writing until that problem resolves), and (B) how much of the profile-derived assumption (audience, register, chosen topic-scope) gets surfaced back to the operator in the restatement -- fully stated for transparency vs. minimal/implicit since the author already knows their own profile. Audience-resolution-from-profile and ask-only-when-degenerate are INVARIANTS in every variant, not points of comparison. Each variant states its weakest_link.

## Blast Radius

internal/editorial/** (new pass + Precondition check), internal/mcpserver (new MCP tool(s) mirroring the save_*_template pattern), cmd/pysar/assets/claude/skills/ps-intake/SKILL.md (new skill), indirectly touches the still-undecided artifact/piece-structure layer (prob-20260718-f6ac8127) and optionally the memory engine (dec-20260718-404573a2) for topic-repeat avoidance

## Reversibility

medium

## Related History

- [refresh report] **Refresh: prob-20260725-53b2d8ea** `ref-20260725-f420c273`
- [refresh report] **Refresh: sol-20260725-3bc0a8f0** `ref-20260725-37805ce9`
- [decision] **Manifest-only machine layer; zero prose templates at init** `dec-20260718-4b222794`
