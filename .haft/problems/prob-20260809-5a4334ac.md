---
id: prob-20260809-5a4334ac
kind: ProblemCard
version: 2
status: active
title: Cursor cold-path: init'd Pysar MCP never becomes usable for end authors
mode: standard
created_at: 2026-08-09T08:48:48Z
updated_at: 2026-08-09T08:53:05Z
---

# Cursor cold-path: init'd Pysar MCP never becomes usable for end authors

## Signal

Live dogfood on Cursor 3.15.6 (Customize → MCPs): after pysar init --cursor, project .cursor/mcp.json is present with command=pysar / serve / PYSAR_PROJECT_ROOT=${workspaceFolder}, but the pysar server stays status=disconnected (Cursor logs: project-0-test-cursor-pysar none→disconnected; createClient only for Cloudflare marketplace plugins; no stdio spawn). Customize 'Connected' lists only Plugin MCPs. macOS Dock-launched Cursor GUI PATH lacks ~/.local/bin, so bare 'pysar' cannot start even when toggled. End-author recovery today requires absolute command path, workspace-scope filter hunting, and/or MCP Logs — not a product path. This is not 'operator forgot a step'; it is the cold path our Cursor host DRR (dec-20260808-f3001106) claims to ship.

## Problem Type

synthesis

## Constraints

- Preserve hostAdapter + shared skill corpus (dec-20260808-f3001106); Claude and Codex scaffolds must stay green
- Piece persistence remains via pysar MCP tools (ps-* mechanical I/O contract), not agent raw Write/Bash of .pysar/**
- No requirement that the author be a developer who understands MCP, PATH, or Cursor Customize scopes
- Do not solve by shipping a 'open MCP Logs and fix absolute path' runbook as the primary product answer

## Optimization Targets

- Cold-path success rate: Dock-launched Cursor + documented install → first /ps intake works without config surgery
- Time-to-first-successful-MCP-tool after init (minutes, not debug sessions)

## Observation Indicators

- Whether authors eventually discover Customize workspace filter (watch, do not optimize for educating them about Cursor internals)
- Number of support/debug loops on Discord/issues about 'MCP not connected'

## Acceptance

DRAFT for operator edit: On a clean macOS machine with pysar installed only via the documented install path (binary on ~/.local/bin), run `pysar init --cursor <dir>`, open <dir> in Cursor launched from Dock (not from a shell with enriched PATH), enable any single first-run Cursor prompt if shown, then invoke `/ps` with a one-sentence idea. Observable success: pysar MCP tools are available to the agent AND a provisional piece appears under <dir>/.pysar/pieces/*/brief.md written via MCP — with zero hand-edits to mcp.json, zero Customize filter archaeology, and zero MCP Logs debugging.

## Blast Radius

cmd/pysar Cursor host scaffold (assets/cursor/mcp.json, possibly init-time rewrite), docs/journey Cursor path, maybe site install copy; Cursor author experience; may touch install packaging if we change how binary is located

## Reversibility

medium

## Related History

- [decision] **Isolated site/ package + GHA Direct Upload (recommended shape)** `dec-20260808-da785647`
- [decision] **Shared skill core + hostAdapter registry (V3 trigger)** `dec-20260808-f3001106`
- [decision] **Homepage / HTML-first signals (meta, visible AI block, JSON-LD)** `dec-20260808-homepage-ai-install-signals-9eb5a6a9`

## Characterization v1 (2026-08-09)

| Dimension | Role | Scale | Unit | Polarity | Measurement |
|-----------|------|-------|------|----------|-------------|
| discoverability | target | ordinal | 1-5 | higher_better | 1=author must know buried Cursor UI lore; 3=one explicit init-printed step with clear locus; 5=appears in Customize Connected / one-click Add like marketplace plugins without hunting workspace filters |
| spawn_reliability | constraint | ordinal | 1-5 | higher_better | Hard floor: Dock-launched Cursor must be able to spawn pysar serve once the server is enabled. 1=bare command fails GUI PATH; 3=resolved path/install PATH usually works; 5=path contract proven across documented install locations. Variants scoring <3 on this constraint are eliminated before target ranking. |
| mcp_io_contract | constraint | ordinal | pass_fail | higher_better | pass=piece persistence still via pysar MCP tools without requiring a parallel CLI I/O skill path; fail=cold path depends on non-MCP Write/CLI dual path that supersedes MCP-only mechanical I/O without a new DRR. Fail eliminates. |
| ship_latency | target | ordinal | 1-5 | lower_better | 1=days (init+docs+tests); 3=weeks (deeplink+packaging polish); 5=marketplace review / multi-host skill rewrite / months |
| dual_surface_drift | observation | ordinal | 1-5 | lower_better | Watch only: how many install/config surfaces must stay in sync (project mcp.json, marketplace plugin, CLI fallback, PATH installer). Anti-Goodhart — do not optimize away a high-discoverability option solely to minimize this. |
