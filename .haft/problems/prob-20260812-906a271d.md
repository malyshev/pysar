---
id: prob-20260812-906a271d
kind: ProblemCard
version: 2
status: active
title: Configurable export destination for finished pieces
context: pipeline
mode: standard
created_at: 2026-08-12T12:54:57Z
updated_at: 2026-08-12T12:55:22Z
---

# Configurable export destination for finished pieces

## Signal

Finished pieces always land as <project root>/<slug>.md via WriteToRoot / export_piece_to_root. Operators want to choose where finished Markdown lands: (1) set an export directory at pysar init, (2) change that setting later in project config without re-init, (3) MCP must expose the resolved landing path so agents write/export to the configured place instead of assuming repo root.

## Problem Type

synthesis

## Constraints

- Default remains project root when unset (backward compatible with existing projects and docs that say root)
- Export destination is a project-level setting, not a global ~/.pysar preference
- Path must be constrained to inside the project (no escape outside project root) unless an explicit later decision allows otherwise
- Compose with pending mechanical citation resolve at export (dec-20260810-export-resolve-citations-6afd8c31) — destination change must not block that work
- Init stays non-interactive flags (Cobra flags), consistent with existing --claude/--cursor/--codex style
- Pysar output discipline: no noise explaining absences; MCP/skill text must tell agents the configured path, not narrate skips

## Optimization Targets

- Operator control of finished-piece landing path without leaving project root littered
- Agent correctness: MCP returns where the file actually landed

## Observation Indicators

- How often operators still want a one-off override per piece
- Whether tool rename (export_piece_to_root → export_piece) causes host/skill churn

## Acceptance

After init with an export-dir option (or after editing the project config to a non-root relative path), export_piece_to_root (or successor) writes <configured-dir>/<slug>.md, creates the directory if missing, returns the absolute or project-relative destination path in the tool result, and default/omitted config still exports to project root. A post-init config change is honored on the next export without re-init. Observable via Go tests + one MCP call result path.

## Blast Radius

cmd/pysar init + .pysar/project schema; internal/export WriteToRoot; MCP export_piece_to_root (+ possibly rename); /ps and export docs; skills that say "project root"; tests for init/export/mcp

## Reversibility

high

## Related History

- [decision] **Mechanical resolve-at-export from sources.md** `dec-20260810-export-resolve-citations-6afd8c31`
- [Note] **** `note-20260726-8c8a52e8`
- [decision] **Manifest-only machine layer; zero prose templates at init** `dec-20260718-4b222794`

## Characterization v1 (2026-08-12)

| Dimension | Role | Scale | Unit | Polarity | Measurement | Valid Until |
|-----------|------|-------|------|----------|-------------|-------------|
| operator_set_and_forget | constraint | ordinal | 1-5 | higher_better | 1 = no durable project setting; 3 = init sets it OR later edit works but not both cleanly; 5 = init flag AND later change without re-init both work | 2026-11-12 |
| mcp_agent_landing_clarity | constraint | ordinal | 1-5 | higher_better | 1 = agent must guess/hardcode root; 3 = writes to configured place but result path ambiguous; 5 = MCP uses config (and optional override) and returns resolved dest path agents can cite | 2026-11-12 |
| implementation_surface | target | ordinal | 1-5 | higher_better | 1 = new subsystem or breaking rename wave; 3 = flag+field+docs+tests; 5 = tiny WriteToRoot-only change with no schema | 2026-11-12 |
| backward_compat | constraint | ordinal | 1-5 | higher_better | 1 = existing projects/skills break or root default disappears; 5 = absent export_dir still means project root; tool name can stay | 2026-11-12 |
| one_off_override_demand | observation | ordinal | 1-5 | higher_better | Watch whether authors need one-off redirects often; do not maximize override knobs | 2026-11-12 |

**Parity plan:**
- Baseline set: V1, V2, V3, V4, V5
- Window: MVP: durable project landing path for finished Markdown
- Budget: Same WriteToRoot + MCP export path; no citation-resolve implementation in this decision
- Missing data policy: explicit_abstain
